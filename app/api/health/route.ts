import {NextResponse} from 'next/server';import {connectMongo} from '@/lib/db';export async function GET(){try{await connectMongo();return NextResponse.json({ok:true,database:'connected'});}catch(e){return NextResponse.json({ok:false,database:'unreachable',error:'Database unavailable'},{status:503});}}
