'use client';
import {useEffect,useRef,useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';import {Command,LogOut,MapPinned,Search,Shield,User as UserIcon} from 'lucide-react';import {useAuth} from '@/lib/client-auth';import {Brand} from '@/components/Brand';import {Avatar} from '@/components/Plate';import {Button} from '@/components/ui';
export function AppNavbar({onOpenSearch}:{onOpenSearch?:()=>void}){const{user,logout}=useAuth();const router=useRouter();const[open,setOpen]=useState(false);const ref=useRef<HTMLDivElement>(null);useEffect(()=>{if(!open)return;const close=(e:PointerEvent)=>{if(!ref.current?.contains(e.target as Node))setOpen(false)};document.addEventListener('pointerdown',close);return()=>document.removeEventListener('pointerdown',close)},[open]);const name=`${user?.first_name??''} ${user?.last_name??''}`.trim()||'Traveller';return <header className="sticky top-0 z-40 border-b border-rule bg-surface/95 backdrop-blur-md"><div className="mx-auto flex h-16 max-w-[1280px] items-center justify-between gap-3 px-4 sm:px-6"><Link href="/dashboard" aria-label="GlobeTrotter dashboard" className="shrink-0"><Brand/></Link><button onClick={onOpenSearch} className="hidden md:flex h-10 w-72 items-center gap-2.5 rounded-xl border border-rule bg-canvas-subtle px-3.5 text-[13px] text-slate transition hover:border-brand/40 hover:text-ink focus-visible:ring-2 focus-visible:ring-brand/30" aria-label="Search destinations and trips"><Search size={15}/><span className="flex-1 text-left">Search destinations, trips...</span><span className="rounded-md border bg-surface px-1.5 py-0.5 font-mono text-[10px] text-mist"><Command size={10} className="inline"/> K</span></button><div className="flex items-center gap-2"><Button size="sm" variant="primary" onClick={()=>router.push('/trips/new')} className="hidden sm:inline-flex"><MapPinned size={14}/>Plan new trip</Button><div className="relative" ref={ref}><button onClick={()=>setOpen(v=>!v)} className="rounded-full p-1 ring-offset-2 transition hover:opacity-80 focus-visible:ring-2 focus-visible:ring-brand" aria-label="Open account menu" aria-expanded={open}><Avatar name={name} src={user?.photo_url} size={34}/></button>{open&&<div className="rise card-elevated absolute right-0 top-12 w-64 overflow-hidden p-1.5"><div className="border-b border-rule-subtle px-3 py-3"><p className="truncate text-sm font-bold">{name}</p><p className="truncate text-xs text-mist">{user?.email}</p><span className="mt-2 inline-flex rounded-full bg-brand-soft px-2 py-0.5 text-[10px] font-bold uppercase tracking-wider text-brand">{user?.role==='admin'?'Administrator':'Explorer'}</span></div><Link href="/profile" onClick={()=>setOpen(false)} className="flex items-center gap-2.5 rounded-lg px-3 py-2.5 text-sm hover:bg-canvas focus-visible:bg-canvas"><UserIcon size={15}/>Profile & settings</Link>{user?.role==='admin'&&<Link href="/admin" onClick={()=>setOpen(false)} className="flex items-center gap-2.5 rounded-lg px-3 py-2.5 text-sm hover:bg-canvas focus-visible:bg-canvas"><Shield size={15}/>Admin console</Link>}<div className="my-1 border-t border-rule-subtle"/><button onClick={()=>{setOpen(false);logout();router.push('/login')}} className="flex w-full items-center gap-2.5 rounded-lg px-3 py-2.5 text-left text-sm text-flag hover:bg-flag-soft focus-visible:bg-flag-soft"><LogOut size={15}/>Sign out</button></div>}</div></div></div></header>}
